# OSV-Scanner configuration
# https://google.github.io/osv-scanner/configuration/
#
# This file documents intentionally-suppressed vulnerability findings.
# Each entry MUST include a `reason` (justification) and SHOULD include
# an `ignoreUntil` date so the suppression auto-expires and the finding
# resurfaces if it remains relevant.
#
# Currently no findings are suppressed. If a finding needs to be ignored,
# add an [[IgnoredVulns]] block below with a `reason` field justifying
# the suppression. See git history for an example of past suppressions
# (e.g., GHSA-v2v4-37r5-5v8g for ip-address@10.1.0, resolved by bumping
# express-rate-limit to 8.5.1 in v2.0.2).
